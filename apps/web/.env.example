# Copie para .env (git-ignorado) se for rodar o frontend fora do Docker
# Compose — dentro do Compose, VITE_API_URL já vem do .env da raiz do repo
# (ver docker-compose.yml, serviço "web").
VITE_API_URL=http://localhost:3000
